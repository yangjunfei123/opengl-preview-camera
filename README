# opengl-preview-camera
Use opengl ES implements preview camera in background, read RGBA data from camera.

1. Use Open GL do camera preview.
2. support read camera data in background, data format is RGBA.


1. 使用Opengl 进行camera的预览。
2. 使用opengl 读取camera的数据，camera数据的格式为RGBA。


